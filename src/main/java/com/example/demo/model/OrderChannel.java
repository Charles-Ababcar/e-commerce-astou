package com.example.demo.model;

public enum OrderChannel {
    WEB,        // Commande classique via le bouton "Confirmer"
    WHATSAPP,   // Commande initiée via le bouton "WhatsApp"
    MOBILE      // (Optionnel) pour plus tard
}
