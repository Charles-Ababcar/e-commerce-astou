spring:
  datasource:
    url: jdbc:postgresql://postgres_test_db:5432/minane_test?connectTimeout=5000&socketTimeout=5000
    username: test_user
    password: TestPassword2024!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  logging:
    level:
      root: INFO
      com.example.demo: DEBUG
